name: Gelato Monitor

on:
  schedule:
    - cron: '*/5 * * * *' # Scheduled to check Gelato House status every 5 minutes
  workflow_dispatch: # Allow manual triggering

jobs:
  monitor:
    runs-on: ubuntu-latest
    permissions:
      contents: write # Needed to push state.json back to the repo
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: pastel-theme

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Run Monitor Script
        env:
          ARVAN_WORKER_URL: ${{ secrets.ARVAN_WORKER_URL }}
        run: python monitor.py --token "${{ secrets.TELEGRAM_BOT_TOKEN }}" --chats "${{ secrets.TELEGRAM_CHAT_ID }}"

      - name: Commit and Push state.json
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add state.json
          git diff --quiet && git diff --staged --quiet || (git commit -m "chore: update gelato status" && git push origin HEAD:pastel-theme)
